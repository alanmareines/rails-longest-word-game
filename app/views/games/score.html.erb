<h1>Result</h1>

<% if @result[:message] == "not an english word" %>
  <p>Sorry, but <em><%= params[:word].upcase %></em> can't be built out of <%= params[:grid] %></p>
  <% elsif @result[:score] == 0 %>
    <p>Sorry, but <em><%= params[:word].upcase %></em> is not an English word...</p>
  <% else %>
    <p><strong>Congratulations!</strong> <em><%= params[:word].upcase %></em>  is a valid English Word. <br>Your score is <strong><%= @result[:score] %></strong></p>
<% end %>

<%= link_to 'Try again you dumb fuck', new_path %>